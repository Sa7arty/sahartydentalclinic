[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: send every route to index.html so React Router can handle it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
